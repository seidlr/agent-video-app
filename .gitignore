node_modules
dist
*.mcpb
# Regenerated on every `npm run build` (or a direct `tsx scripts/build-skill.ts` run) from
# skills/agent-video-studio/ + the live tool registry -- same "build output, not source" status
# as dist/ itself, and would otherwise be pure diff noise (digests shift on every tool added).
public/.well-known/
public/skill.zip
playwright-report
test-results
.open-claude-design/
docs/plans/.annotations/
*.local
*.log
*.tsbuildinfo
.DS_Store
.claude/scheduled_tasks.lock
.codegraph/
