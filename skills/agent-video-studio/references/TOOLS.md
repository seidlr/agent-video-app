# Tool reference

_Generated by `scripts/build-skill.ts` from the live tool registry -- do not hand-edit._

Every tool resolves to `{ok:true, summary, ...}` or `{ok:false, error, hint?}`, never an
exception. `when` says when a tool is registered: `always` (registers immediately),
`local` (only while a decodable local/URL source is loaded), `yt` (also registers for a
YouTube source), `after-transcribe` (only once a transcript exists). `mode: job` tools accept
the universal `wait`/`waitSeconds`/`requestId` envelope fields on top of their own arguments.

60 tools:

## `add_box`

**Group:** boxes · **When:** always

Draws a box overlay at a point in time (or a range, with `until`). Coordinates are normalized [0,1] fractions of the frame.

| Argument | Type | Required |
|---|---|---|
| `time` | string or number | yes |
| `until` | string or number | no |
| `x` | number | yes |
| `y` | number | yes |
| `w` | number | yes |
| `h` | number | yes |
| `label` | string | yes |

Example call:

```json
{
  "name": "add_box",
  "arguments": {
    "time": "<time>",
    "until": "<until>",
    "x": "<x>",
    "y": "<y>",
    "w": "<w>",
    "h": "<h>",
    "label": "<label>"
  }
}
```

## `add_chapter`

**Group:** chapters · **When:** always

Adds a chapter (a titled time range). Rejects a range that overlaps an existing chapter.

| Argument | Type | Required |
|---|---|---|
| `start` | string or number | yes |
| `end` | string or number | yes |
| `title` | string | yes |

Example call:

```json
{
  "name": "add_chapter",
  "arguments": {
    "start": "<start>",
    "end": "<end>",
    "title": "<title>"
  }
}
```

## `add_clip`

**Group:** clips · **When:** always

Adds a non-destructive clip range for export_video/export_gif. Clips are ordered by when they were added; use reorder_clips to change that.

| Argument | Type | Required |
|---|---|---|
| `start` | string or number | yes |
| `end` | string or number | yes |
| `name` | string | no |

Example call:

```json
{
  "name": "add_clip",
  "arguments": {
    "start": "<start>",
    "end": "<end>",
    "name": "<name>"
  }
}
```

## `add_note`

**Group:** notes · **When:** always

Adds a timestamped note, either a single point in time or a region (with `end`). Tags are freeform.

| Argument | Type | Required |
|---|---|---|
| `time` | string or number | yes |
| `end` | string or number | no |
| `text` | string | yes |
| `tags` | array | no |

Example call:

```json
{
  "name": "add_note",
  "arguments": {
    "time": "<time>",
    "end": "<end>",
    "text": "<text>",
    "tags": "<tags>"
  }
}
```

## `cancel_job`

**Group:** session · **When:** always

Cancels a running background job.

| Argument | Type | Required |
|---|---|---|
| `jobId` | string | yes |

Example call:

```json
{
  "name": "cancel_job",
  "arguments": {
    "jobId": "<jobId>"
  }
}
```

## `capture_frame`

**Group:** frames · **When:** yt

Captures the exact currently-displayed video frame as an image. Shows it in the Frames tray, and optionally downloads it or returns it as a data URL for a host with no file access.

| Argument | Type | Required |
|---|---|---|
| `time` | string or number | no |
| `format` | "png" or "jpeg" or "webp" | no |
| `maxWidth` | number | no |
| `includeOverlays` | boolean | no |
| `download` | boolean | no |
| `includeDataUrl` | boolean | no |
| `name` | string | no |
| `stay` | boolean | no |

Example call:

```json
{
  "name": "capture_frame",
  "arguments": {
    "time": "<time>",
    "format": "<format>",
    "maxWidth": "<maxWidth>",
    "includeOverlays": "<includeOverlays>",
    "download": "<download>",
    "includeDataUrl": "<includeDataUrl>",
    "name": "<name>",
    "stay": "<stay>"
  }
}
```

## `clear_boxes`

**Group:** boxes · **When:** always

Removes every box.

_No arguments._

Example call:

```json
{
  "name": "clear_boxes",
  "arguments": {}
}
```

## `delete_box`

**Group:** boxes · **When:** always

Removes a box.

| Argument | Type | Required |
|---|---|---|
| `boxId` | string | yes |

Example call:

```json
{
  "name": "delete_box",
  "arguments": {
    "boxId": "<boxId>"
  }
}
```

## `delete_chapter`

**Group:** chapters · **When:** always

Removes a chapter.

| Argument | Type | Required |
|---|---|---|
| `chapterId` | string | yes |

Example call:

```json
{
  "name": "delete_chapter",
  "arguments": {
    "chapterId": "<chapterId>"
  }
}
```

## `delete_frame`

**Group:** frames · **When:** always

Removes a captured frame from the Frames tray.

| Argument | Type | Required |
|---|---|---|
| `frameId` | string | no |

Example call:

```json
{
  "name": "delete_frame",
  "arguments": {
    "frameId": "<frameId>"
  }
}
```

## `delete_note`

**Group:** notes · **When:** always

Removes a note from the timeline.

| Argument | Type | Required |
|---|---|---|
| `noteId` | string | yes |

Example call:

```json
{
  "name": "delete_note",
  "arguments": {
    "noteId": "<noteId>"
  }
}
```

## `detect_faces`

**Group:** vision · **When:** local · **Mode:** job

Detects faces at a frame (MediaPipe BlazeFace short-range): box + 6 keypoints per face. First call without confirmDownload to see the model size.

| Argument | Type | Required |
|---|---|---|
| `time` | string or number | no |
| `addBoxes` | boolean | no |
| `confirmDownload` | boolean | no |

Example call:

```json
{
  "name": "detect_faces",
  "arguments": {
    "time": "<time>",
    "addBoxes": "<addBoxes>",
    "confirmDownload": "<confirmDownload>"
  }
}
```

## `detect_objects`

**Group:** vision · **When:** local · **Mode:** job

Detects objects in a frame: closed-set (RF-DETR-nano on WebGPU / YOLOS-tiny on wasm) by default, or zero-shot (Grounding-DINO) when `labels` is given (e.g. ["a red car", "a person"]). First call without confirmDownload to see the model size.

| Argument | Type | Required |
|---|---|---|
| `time` | string or number | no |
| `labels` | array | no |
| `threshold` | number | no |
| `addBoxes` | boolean | no |
| `confirmDownload` | boolean | no |

Example call:

```json
{
  "name": "detect_objects",
  "arguments": {
    "time": "<time>",
    "labels": "<labels>",
    "threshold": "<threshold>",
    "addBoxes": "<addBoxes>",
    "confirmDownload": "<confirmDownload>"
  }
}
```

## `detect_pose`

**Group:** vision · **When:** local · **Mode:** job

Detects body pose landmarks at a frame (MediaPipe pose-landmarker-lite): 33 normalized landmarks per detected person. First call without confirmDownload to see the model size.

| Argument | Type | Required |
|---|---|---|
| `time` | string or number | no |
| `addBoxes` | boolean | no |
| `confirmDownload` | boolean | no |

Example call:

```json
{
  "name": "detect_pose",
  "arguments": {
    "time": "<time>",
    "addBoxes": "<addBoxes>",
    "confirmDownload": "<confirmDownload>"
  }
}
```

## `detect_scenes`

**Group:** vision · **When:** local · **Mode:** job

Detects scene/shot boundaries via color histogram analysis. Optionally adds a chapter ("Scene N") per detected scene.

| Argument | Type | Required |
|---|---|---|
| `addChapters` | boolean | no |
| `sensitivity` | number | no |
| `minSceneDuration` | number | no |

Example call:

```json
{
  "name": "detect_scenes",
  "arguments": {
    "addChapters": "<addChapters>",
    "sensitivity": "<sensitivity>",
    "minSceneDuration": "<minSceneDuration>"
  }
}
```

## `estimate_depth`

**Group:** vision · **When:** local · **Mode:** job

Estimates monocular depth at a frame (Depth Anything v2). format:"stats" (default) returns near/far/mean/variance and an inferred shotType (close-up/medium/wide); format:"image" also adds a colorized depth map to the Frames tray. First call without confirmDownload to see the model size.

| Argument | Type | Required |
|---|---|---|
| `time` | string or number | no |
| `format` | "image" or "stats" | no |
| `confirmDownload` | boolean | no |

Example call:

```json
{
  "name": "estimate_depth",
  "arguments": {
    "time": "<time>",
    "format": "<format>",
    "confirmDownload": "<confirmDownload>"
  }
}
```

## `export_gif`

**Group:** export · **When:** local · **Mode:** job

Exports a time range as a GIF (default 320px wide, 10fps). Capped at 300 frames -- fails with too_many_frames if the range/fps combination would exceed that.

| Argument | Type | Required |
|---|---|---|
| `start` | string or number | yes |
| `end` | string or number | yes |
| `width` | number | no |
| `fps` | number | no |
| `download` | boolean | no |

Example call:

```json
{
  "name": "export_gif",
  "arguments": {
    "start": "<start>",
    "end": "<end>",
    "width": "<width>",
    "fps": "<fps>",
    "download": "<download>"
  }
}
```

## `export_notes`

**Group:** export · **When:** always

Exports notes/chapters/boxes as markdown, json, vtt, srt, csv, or edl text. The full text is always in the result -- download/copyToClipboard are optional extra delivery channels for a host with file access or clipboard access instead of (or in addition to) reading the JSON result.

| Argument | Type | Required |
|---|---|---|
| `format` | "markdown" or "json" or "vtt" or "srt" or "csv" or "edl" | yes |
| `download` | boolean | no |
| `copyToClipboard` | boolean | no |

Example call:

```json
{
  "name": "export_notes",
  "arguments": {
    "format": "<format>",
    "download": "<download>",
    "copyToClipboard": "<copyToClipboard>"
  }
}
```

## `export_project`

**Group:** export · **When:** always

Exports the current project (notes, chapters, boxes, tracks, clips, captured frames, and a transcript if one exists) as a .zip -- notes.md and a project.json alongside frames/*.png. Re-import it via the Library panel's "Import project" button. Metadata only for now: the source video itself is not bundled.

| Argument | Type | Required |
|---|---|---|
| `download` | boolean | no |

Example call:

```json
{
  "name": "export_project",
  "arguments": {
    "download": "<download>"
  }
}
```

## `export_video`

**Group:** export · **When:** local · **Mode:** job

Trims and concatenates clips (added via add_clip) into a single mp4/webm file, optionally burning box overlays into the video. First call may take a while for long clips -- this is a job-mode tool; poll with get_job if it does not finish inline.

| Argument | Type | Required |
|---|---|---|
| `clips` | string or array | yes |
| `format` | "mp4" or "webm" | no |
| `width` | number | no |
| `burnOverlays` | boolean | no |
| `download` | boolean | no |

Example call:

```json
{
  "name": "export_video",
  "arguments": {
    "clips": "<clips>",
    "format": "<format>",
    "width": "<width>",
    "burnOverlays": "<burnOverlays>",
    "download": "<download>"
  }
}
```

## `find_similar_frames`

**Group:** vision · **When:** local · **Mode:** job

Finds time ranges visually similar to a reference frame (by captured frameId or a timestamp). Default `method:"hash"` uses a color-aware perceptual hash (no model download); `method:"dino"` uses DINOv3 patch-feature cosine similarity, more robust to lighting/angle changes but needs a model download.

| Argument | Type | Required |
|---|---|---|
| `frameId` | string | no |
| `time` | string or number | no |
| `maxDistance` | number | no |
| `maxColorDistance` | number | no |
| `method` | "hash" or "dino" | no |
| `minScore` | number | no |
| `confirmDownload` | boolean | no |

Example call:

```json
{
  "name": "find_similar_frames",
  "arguments": {
    "frameId": "<frameId>",
    "time": "<time>",
    "maxDistance": "<maxDistance>",
    "maxColorDistance": "<maxColorDistance>",
    "method": "<method>",
    "minScore": "<minScore>",
    "confirmDownload": "<confirmDownload>"
  }
}
```

## `find_speaker_turns`

**Group:** audio · **When:** local · **Mode:** job

Detects speaker-change turns in the audio (pyannote speaker diarization), merging consecutive same-speaker segments separated by a gap under 0.3s. Optionally adds a region note per turn.

| Argument | Type | Required |
|---|---|---|
| `from` | string or number | no |
| `to` | string or number | no |
| `addNotes` | boolean | no |
| `confirmDownload` | boolean | no |

Example call:

```json
{
  "name": "find_speaker_turns",
  "arguments": {
    "from": "<from>",
    "to": "<to>",
    "addNotes": "<addNotes>",
    "confirmDownload": "<confirmDownload>"
  }
}
```

## `generate_thumbnails`

**Group:** frames · **When:** local · **Mode:** job

Generates a thumbnail sprite for the loaded video and wires it into the timeline hover preview. Give either count (evenly spread) or intervalSeconds (fixed spacing).

| Argument | Type | Required |
|---|---|---|
| `count` | integer | no |
| `intervalSeconds` | number | no |
| `contactSheet` | boolean | no |

Example call:

```json
{
  "name": "generate_thumbnails",
  "arguments": {
    "count": "<count>",
    "intervalSeconds": "<intervalSeconds>",
    "contactSheet": "<contactSheet>"
  }
}
```

## `get_agent_skill`

**Group:** session · **When:** always

Returns the agent skill documentation (SKILL.md) describing how to use this app's tools -- workflows, the tool response envelope, the job protocol, model-download gating, and safety notes. Pass `section` (a heading, e.g. "Workflows") to get just that part instead of the whole document.

| Argument | Type | Required |
|---|---|---|
| `section` | string | no |

Example call:

```json
{
  "name": "get_agent_skill",
  "arguments": {
    "section": "<section>"
  }
}
```

## `get_job`

**Group:** session · **When:** always

Reports the status, progress and (once finished) result of a background job started by a job-mode tool.

| Argument | Type | Required |
|---|---|---|
| `jobId` | string | yes |

Example call:

```json
{
  "name": "get_job",
  "arguments": {
    "jobId": "<jobId>"
  }
}
```

## `get_state`

**Group:** session · **When:** always

Reports the full current studio state: the loaded source, player transport, item counts, capabilities, and hints for what to do next. Call this first.

_No arguments._

Example call:

```json
{
  "name": "get_state",
  "arguments": {}
}
```

## `get_transcript`

**Group:** transcript · **When:** after-transcribe

Returns the transcript (optionally trimmed to a time range) as segments, plain text, SRT, or VTT.

| Argument | Type | Required |
|---|---|---|
| `from` | string or number | no |
| `to` | string or number | no |
| `format` | "text" or "segments" or "srt" or "vtt" | no |
| `lang` | string | no |

Example call:

```json
{
  "name": "get_transcript",
  "arguments": {
    "from": "<from>",
    "to": "<to>",
    "format": "<format>",
    "lang": "<lang>"
  }
}
```

## `import_vtt`

**Group:** chapters · **When:** always

Imports chapters from a WEBVTT string, adding every cue that does not overlap an existing chapter.

| Argument | Type | Required |
|---|---|---|
| `vtt` | string | yes |

Example call:

```json
{
  "name": "import_vtt",
  "arguments": {
    "vtt": "<vtt>"
  }
}
```

## `list_boxes`

**Group:** boxes · **When:** always

Lists boxes, optionally filtered to only those visible at a given time.

| Argument | Type | Required |
|---|---|---|
| `time` | string or number | no |

Example call:

```json
{
  "name": "list_boxes",
  "arguments": {
    "time": "<time>"
  }
}
```

## `list_chapters`

**Group:** chapters · **When:** always

Lists every chapter, sorted by start time.

_No arguments._

Example call:

```json
{
  "name": "list_chapters",
  "arguments": {}
}
```

## `list_clips`

**Group:** clips · **When:** always

Lists the clips currently on the timeline, in export order.

_No arguments._

Example call:

```json
{
  "name": "list_clips",
  "arguments": {}
}
```

## `list_frames`

**Group:** frames · **When:** always

Lists every frame captured this session (the Frames tray).

_No arguments._

Example call:

```json
{
  "name": "list_frames",
  "arguments": {}
}
```

## `list_jobs`

**Group:** session · **When:** always

Lists every background job started this session, most recent last.

_No arguments._

Example call:

```json
{
  "name": "list_jobs",
  "arguments": {}
}
```

## `list_library`

**Group:** library · **When:** always

Lists the sample catalog and every video stored locally in this browser.

_No arguments._

Example call:

```json
{
  "name": "list_library",
  "arguments": {}
}
```

## `list_models`

**Group:** models · **When:** always

Lists every model this app can load: size, license, and whether it is cached locally or currently loaded in memory.

_No arguments._

Example call:

```json
{
  "name": "list_models",
  "arguments": {}
}
```

## `list_notes`

**Group:** notes · **When:** always

Lists every note on the timeline, sorted by time.

_No arguments._

Example call:

```json
{
  "name": "list_notes",
  "arguments": {}
}
```

## `load_model`

**Group:** models · **When:** always · **Mode:** job

Downloads (if not already cached) and loads a model into memory, ready for the tools that use it. First call without confirmDownload to see the size; pass confirmDownload:true to actually download.

| Argument | Type | Required |
|---|---|---|
| `id` | string | yes |
| `confirmDownload` | boolean | no |

Example call:

```json
{
  "name": "load_model",
  "arguments": {
    "id": "<id>",
    "confirmDownload": "<confirmDownload>"
  }
}
```

## `load_video`

**Group:** library · **When:** always

Loads a video into the player: a sample by id, a video already in your library by id, a CORS-enabled URL, or a YouTube link.

| Argument | Type | Required |
|---|---|---|
| `source` | "sample" or "library" or "url" or "youtube" | yes |
| `id` | string | no |
| `url` | string | no |

Example call:

```json
{
  "name": "load_video",
  "arguments": {
    "source": "<source>",
    "id": "<id>",
    "url": "<url>"
  }
}
```

## `pause`

**Group:** playback · **When:** always

Pauses playback.

_No arguments._

Example call:

```json
{
  "name": "pause",
  "arguments": {}
}
```

## `play`

**Group:** playback · **When:** always

Starts (or resumes) playback.

_No arguments._

Example call:

```json
{
  "name": "play",
  "arguments": {}
}
```

## `remove_clip`

**Group:** clips · **When:** always

Removes a clip.

| Argument | Type | Required |
|---|---|---|
| `clipId` | string | yes |

Example call:

```json
{
  "name": "remove_clip",
  "arguments": {
    "clipId": "<clipId>"
  }
}
```

## `remove_video`

**Group:** library · **When:** always

Deletes a stored video from the library (its bytes and metadata).

| Argument | Type | Required |
|---|---|---|
| `id` | string | yes |

Example call:

```json
{
  "name": "remove_video",
  "arguments": {
    "id": "<id>"
  }
}
```

## `reorder_clips`

**Group:** clips · **When:** always

Reorders clips (and therefore their concatenation order in export_video) to match the given list of clip ids.

| Argument | Type | Required |
|---|---|---|
| `order` | array | yes |

Example call:

```json
{
  "name": "reorder_clips",
  "arguments": {
    "order": "<order>"
  }
}
```

## `request_file_upload`

**Group:** library · **When:** always

Switches to the Library panel and focuses the local-file input, so an agent with a file-upload capability (or the human) can attach a video.

_No arguments._

Example call:

```json
{
  "name": "request_file_upload",
  "arguments": {}
}
```

## `say`

**Group:** session · **When:** always

Posts a message from the agent into the Activity feed, visible to the human watching the page.

| Argument | Type | Required |
|---|---|---|
| `message` | string | yes |

Example call:

```json
{
  "name": "say",
  "arguments": {
    "message": "<message>"
  }
}
```

## `search_frames`

**Group:** vision · **When:** local · **Mode:** job

Finds time ranges matching a natural-language description (MobileCLIP text-to-image search), e.g. "a solid red image" or "a person talking". First call without confirmDownload to see the model size; the first call for a given video also builds its search index (may take a while for a long video).

| Argument | Type | Required |
|---|---|---|
| `query` | string | yes |
| `topK` | integer | no |
| `minScore` | number | no |
| `confirmDownload` | boolean | no |

Example call:

```json
{
  "name": "search_frames",
  "arguments": {
    "query": "<query>",
    "topK": "<topK>",
    "minScore": "<minScore>",
    "confirmDownload": "<confirmDownload>"
  }
}
```

## `search_transcript`

**Group:** transcript · **When:** after-transcribe

Case-insensitive substring search over the transcript, returning each hit with its timestamp and neighboring segments for context.

| Argument | Type | Required |
|---|---|---|
| `query` | string | yes |
| `lang` | string | no |

Example call:

```json
{
  "name": "search_transcript",
  "arguments": {
    "query": "<query>",
    "lang": "<lang>"
  }
}
```

## `seek`

**Group:** playback · **When:** always

Moves the playhead. Accepts seconds (12.5), a timecode ("1:23.400"), a relative offset ("+5", "-2"), a percent of duration ("50%"), or a frame ("f30").

| Argument | Type | Required |
|---|---|---|
| `time` | string or number | yes |

Example call:

```json
{
  "name": "seek",
  "arguments": {
    "time": "<time>"
  }
}
```

## `segment`

**Group:** vision · **When:** local · **Mode:** job

Segments an object at a point or box prompt (EdgeTAM on WebGPU, SlimSAM on wasm), adding the resulting mask + tight bounding box as a new box. First call without confirmDownload to see the model size.

| Argument | Type | Required |
|---|---|---|
| `time` | string or number | no |
| `points` | array | no |
| `box` | object | no |
| `label` | string | no |
| `confirmDownload` | boolean | no |
| `stay` | boolean | no |

Example call:

```json
{
  "name": "segment",
  "arguments": {
    "time": "<time>",
    "points": "<points>",
    "box": "<box>",
    "label": "<label>",
    "confirmDownload": "<confirmDownload>",
    "stay": "<stay>"
  }
}
```

## `set_playback`

**Group:** playback · **When:** always

Sets volume, mute, playback rate, and/or an in-page A/B loop.

| Argument | Type | Required |
|---|---|---|
| `volume` | number | no |
| `muted` | boolean | no |
| `rate` | number | no |
| `loop` | object or null | no |

Example call:

```json
{
  "name": "set_playback",
  "arguments": {
    "volume": "<volume>",
    "muted": "<muted>",
    "rate": "<rate>",
    "loop": "<loop>"
  }
}
```

## `set_view`

**Group:** session · **When:** always

Switches the right-rail panel and/or the overall layout.

| Argument | Type | Required |
|---|---|---|
| `panel` | "library" or "notes" or "frames" or "tracking" or "vision" or "transcript" or "clips" or "effects" or "models" or "activity" or "skill" | no |
| `layout` | "studio" or "focus" | no |

Example call:

```json
{
  "name": "set_view",
  "arguments": {
    "panel": "<panel>",
    "layout": "<layout>"
  }
}
```

## `step_frames`

**Group:** playback · **When:** local

Steps the playhead by an exact number of frames (negative steps backward). Intended for use while paused.

| Argument | Type | Required |
|---|---|---|
| `count` | integer | yes |

Example call:

```json
{
  "name": "step_frames",
  "arguments": {
    "count": "<count>"
  }
}
```

## `tag_audio_events`

**Group:** audio · **When:** local · **Mode:** job

Tags audio events (speech, music, environmental sounds, ...) over the clip using a 10s sliding window (AST audio classification), merging adjacent windows that agree on a label. Optionally adds a chapter at each label change.

| Argument | Type | Required |
|---|---|---|
| `from` | string or number | no |
| `to` | string or number | no |
| `threshold` | number | no |
| `addChapters` | boolean | no |
| `confirmDownload` | boolean | no |

Example call:

```json
{
  "name": "tag_audio_events",
  "arguments": {
    "from": "<from>",
    "to": "<to>",
    "threshold": "<threshold>",
    "addChapters": "<addChapters>",
    "confirmDownload": "<confirmDownload>"
  }
}
```

## `toggle_play`

**Group:** playback · **When:** always

Plays if paused, pauses if playing.

_No arguments._

Example call:

```json
{
  "name": "toggle_play",
  "arguments": {}
}
```

## `track`

**Group:** vision · **When:** local · **Mode:** job

Tracks an existing box forward in time using template matching (NCC by default), producing keyframes the timeline interpolates between.

| Argument | Type | Required |
|---|---|---|
| `boxId` | string | yes |
| `until` | string or number | no |
| `stepSeconds` | number | no |
| `method` | string | no |

Example call:

```json
{
  "name": "track",
  "arguments": {
    "boxId": "<boxId>",
    "until": "<until>",
    "stepSeconds": "<stepSeconds>",
    "method": "<method>"
  }
}
```

## `transcribe`

**Group:** transcript · **When:** local · **Mode:** job

Transcribes the video's audio with Whisper, adding timestamped segments to the transcript. First call without confirmDownload to see the model size. Omit from/to to transcribe the whole video; a partial range replaces only the overlapping segments.

| Argument | Type | Required |
|---|---|---|
| `model` | "tiny" or "base" | no |
| `from` | string or number | no |
| `to` | string or number | no |
| `language` | string | no |
| `confirmDownload` | boolean | no |

Example call:

```json
{
  "name": "transcribe",
  "arguments": {
    "model": "<model>",
    "from": "<from>",
    "to": "<to>",
    "language": "<language>",
    "confirmDownload": "<confirmDownload>"
  }
}
```

## `unload_model`

**Group:** models · **When:** always

Frees a loaded model from memory (GPU buffers/worker terminated).

| Argument | Type | Required |
|---|---|---|
| `id` | string | yes |

Example call:

```json
{
  "name": "unload_model",
  "arguments": {
    "id": "<id>"
  }
}
```

## `update_box`

**Group:** boxes · **When:** always

Edits an existing box's time, until, coordinates, or label.

| Argument | Type | Required |
|---|---|---|
| `boxId` | string | yes |
| `time` | string or number | no |
| `until` | string or number | no |
| `x` | number | no |
| `y` | number | no |
| `w` | number | no |
| `h` | number | no |
| `label` | string | no |

Example call:

```json
{
  "name": "update_box",
  "arguments": {
    "boxId": "<boxId>",
    "time": "<time>",
    "until": "<until>",
    "x": "<x>",
    "y": "<y>",
    "w": "<w>",
    "h": "<h>",
    "label": "<label>"
  }
}
```

## `update_chapter`

**Group:** chapters · **When:** always

Edits an existing chapter's start, end, or title.

| Argument | Type | Required |
|---|---|---|
| `chapterId` | string | yes |
| `start` | string or number | no |
| `end` | string or number | no |
| `title` | string | no |

Example call:

```json
{
  "name": "update_chapter",
  "arguments": {
    "chapterId": "<chapterId>",
    "start": "<start>",
    "end": "<end>",
    "title": "<title>"
  }
}
```

## `update_note`

**Group:** notes · **When:** always

Edits an existing note's time, end, text, or tags.

| Argument | Type | Required |
|---|---|---|
| `noteId` | string | yes |
| `time` | string or number | no |
| `end` | string or number | no |
| `text` | string | no |
| `tags` | array | no |

Example call:

```json
{
  "name": "update_note",
  "arguments": {
    "noteId": "<noteId>",
    "time": "<time>",
    "end": "<end>",
    "text": "<text>",
    "tags": "<tags>"
  }
}
```

