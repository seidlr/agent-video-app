import { main } from './build-server';

/**
 * Thin entry point for `npm run build:server` -- mirrors `run-build-skill.ts`'s own reasoning for
 * keeping this separate from `build-server.ts` itself.
 */
main();
